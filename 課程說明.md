我修的這門課 是在學傳統資訊檢索的各種基礎技術等 參考的教科書也是著名的introduction to information retrieval 我現在需要開始做相關作業 完成相關的報告還有期末說明 包含最後的projects 我預計之後都已claude code協助我 等一下我會分批次給你我們 教學相關檔案等，然後能請你理解後 搭配我使用claude code的習慣 詳細幫我弄個說明文件給claude code看 讓它知道我要做什麼並協助我完成嗎? 我的偏好其實就跟之前一樣 解釋用中文 不過文件檔案等 我希望是中文為主 不過相關名詞都中英對照 然後程式碼註解等一律英文。 我要強制 claude code 不要每次 有新的進度或是程式碼實作就不斷的新增說明文件， 文件的新增要視情況而定 且同一個主題概念 或之前程式碼的相關修改進步 延伸 不同版本等 都應該直接從舊有文件更新 不應該保有舊的 然後又新增新的 此外 專案要保持清楚的架構化 文黨docs也是 分適當階層subfolders 只要有程式碼實作 程式碼本身基本都要有詳盡的註解以供我有逐行讀程式碼的需求 且每次的各種實作 都要在文檔中說明 特別是背後原理使用方法 當然如我前述說的 很常會遇到 程式碼實作邏輯要修改 或者是 弄得更進階完整的 都一定要是在原來的文檔更新 我希望最後一直持續下去 是文檔保持固定一定數目 但是每個文檔 都很詳盡 很有結構的情況下 此外要強迫claude code不要隨便生成暫時的程式碼檔案等 盡量利用現有的程式碼 或作改進修改來用 不要隨便在tmp 底下 建立文檔程式碼 就只為了一時之用 其他相關的一些設定可參考這份claude.md 結合這些大致上就是我希望claude學習的格式


**總則（務必遵守）：**

1. 所有說明與回覆「中文為主，重要術語中英對照」；**程式碼、註解、檔名與文件中的程式片段一律 English**。
2. 禁止為了「新進度」就新增一堆新文件；**同主題只更新舊檔**，不可保留舊版再多生 *copy(1)*/*final*/*v2* 之類。
3. 專案與 `docs/` 結構保持階層分明；若需新檔，一律走《CLAUDE-IR.md》的既定路徑與命名。
4. 每次實作都要有**最小可執行**（CLI 或 notebook/腳本）與**清楚的英文內嵌註解**；理論與使用方式在原有文件對應段落更新。
5. Commit 採 **Conventional Commits**；同一主題以追加 commit 更新，不開平行文檔。

**立即任務（照順序做）：**
A. **Midterm**：在 `docs/exams/midterm/` 內建立 `OUTLINE.md` → `DRAFT.md`，依《CLAUDE-IR.md》第 10.1 的結構撰寫（中文敘述、術語中英對照）。產出示意圖放 `FIGS/`。最後提供 Markdown→DOCX→PDF 的自動化腳本（可用 pandoc；若無則先給可替代流程）。檔名規則與寄送主旨依文件要求。
B. **CSoundex（中文音似編碼）**：依第 10.2 的**規格**實作

* `src/ir/text/csoundex.py`（`encode` 與 `encode_tokens`）
* `scripts/csoundex_encode.py` CLI
* `tests/test_csoundex.py`（同音聚合、異形字、標點、英文混雜）
* `configs/csoundex.yaml` 可調參的**拼音分組→群碼**表（先依指南預設）。
  不得外連下載資源；若缺拼音字典，先用內嵌小詞表（`datasets/lexicon/basic_pinyin.tsv`）跑測試。
  C. **M6–M9 模組骨架**（見第 11 章）：
* `src/ir/eval/metrics.py`（precision/recall/Fβ、AP、MAP、nDCG）＋ `scripts/eval_run.py`
* `src/ir/ranking/rocchio.py` ＋ `scripts/expand_query.py`（Pseudo-RF/實際 RF）
* `src/ir/cluster/{term_cluster.py,doc_cluster.py}`（complete-link/single-link/star/string + 階層）
* `src/ir/summarize/{static.py,dynamic.py}`（lead-k／關鍵句、KWIC 視窗+快取）
  每個模組都要有**極小**可跑範例與 README 小段（中文解說，術語中英對照；程式內註解英文）。
  D. 只更新既有文件：把理論與「如何使用」補進原文件相對章節，**嚴禁再開新檔**。
  E. 提出**最小三筆**建議 commit（含訊息），然後開始實作 A→B→C，完成一項就發 PR 摘要與跑測（列出可複製的命令）。

**出品品質：**

* 程式碼要可直接執行；測試至少涵蓋正向/邊界；CLI 有 `--help`。
* 文件與圖表用既定階層；圖表一律向量格式（.svg）。
* 報告輸出要能**匹配系上 DOCX 版面**；Midterm 最終輸出：`ACI2025MidTerm<ID>.pdf`（Email 主旨 `ACI2025 Midterm`）。
* 遇到需要新資料夾或依賴時，先在《CLAUDE-IR.md》對應段落**補規格**再實作。


